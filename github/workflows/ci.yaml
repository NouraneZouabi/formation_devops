name: integration contenu spring angular avec github action

on:
  push: 
    branches: main 

jobs: 
  build_and_push: 
    runs-on: ubuntu-latest

    steps: 
    - name: checkout code 
      uses: actions/checkout@v7